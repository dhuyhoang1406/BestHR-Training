# Environment variables example — copy to .env and adjust as needed
PORT=3001
CORS_ORIGIN=http://localhost:3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=besthr
DB_PASSWORD=besthr
DB_NAME=besthr_training
DB_SYNC=true
